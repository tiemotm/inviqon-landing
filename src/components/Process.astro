---
const steps = [
  {
    n: "01",
    title: "Align",
    desc: "We reduce ambiguity: goals, constraints, success metrics.",
  },
  {
    n: "02",
    title: "Build",
    desc: "Small iterations with strong engineering discipline and clean interfaces.",
  },
  {
    n: "03",
    title: "Ship & Improve",
    desc: "Deploy, observe, refine. No dead code. No bloated abstractions.",
  },
];
---

<section class="relative" id="process">
  <div class="mx-auto max-w-6xl px-5">
    <div class="py-16 md:py-24">
      <div class="mx-auto max-w-3xl text-center">
        <p class="reveal text-xs tracking-[0.24em] text-white/55">
          DELIVERY SYSTEM
        </p>

        <h2 class="reveal reveal-2 mt-5 text-3xl font-semibold tracking-tight text-white/95 md:text-5xl">
          A process that stays <span class="accent">lightweight</span>.
        </h2>

        <p class="reveal reveal-3 mx-auto mt-6 max-w-2xl text-base leading-relaxed text-white/65 md:text-lg">
          We work with tight feedback loops and a bias for clarity.
          Engineering choices are made for the future — not just for the demo.
        </p>
      </div>

      <div class="reveal reveal-4 mt-12 grid gap-4 md:grid-cols-3">
        {steps.map((s) => (
          <div class="rounded-2xl glass px-6 py-6">
            <p class="text-xs font-semibold tracking-[0.22em] text-white/45">{s.n}</p>
            <p class="mt-3 text-sm font-semibold text-white/90">{s.title}</p>
            <p class="mt-2 text-sm leading-relaxed text-white/65">{s.desc}</p>
          </div>
        ))}
      </div>

      <div class="mx-auto mt-12 h-px w-24 bg-gradient-to-r from-transparent via-white/20 to-transparent"></div>
    </div>
  </div>
</section>